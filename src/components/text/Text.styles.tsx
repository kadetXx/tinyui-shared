import { styled } from "../../stitches";

export const StyledText = styled("span", {
  color: "$primary",
  fontFamily: "$default",
  display: "inline-block",
  textDecoration: "none",
  margin: 0,
});
